ТекстКода = ЗакодироватьВУРЛ(ТекстКода); 
		ФайлОтвет = ПолучитьИмяВременногоФайла();
		Соединение = новый HTTPСоединение("api.qrserver.com",,,,,,новый ЗащищенноеСоединениеOpenSSL);
		Запрос = новый HTTPЗапрос("v1/create-qr-code/?data="+ТекстКода+"&size=100x100");
		Ответ = Соединение.Получить(Запрос,ФайлОтвет);
		ДД = новый ДвоичныеДанные(ФайлОтвет);
		Хранилище = новый ХранилищеЗначения(ДД); 
		Картинка = новый Картинка(ДД);
		Адрес = ПоместитьВоВременноеХранилище(ДД);
		Шапка.Рисунки.D1.Картинка = Картинка;

&НаСервере
Функция ЗакодироватьВУРЛ(Адрес)
	Возврат КодироватьСтроку(Адрес,СпособКодированияСтроки.КодировкаURL);
КонецФункции